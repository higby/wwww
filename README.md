# higby.github.io

## post front matter

No need to define the layout, all posts use same layout just with different setups:

Example Page Setup:
>setup:
  table: hidden
  date: hidden
  style: ninties
  override: true
redirect_from:
  - /example_page.html/
